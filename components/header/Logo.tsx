import React from "react"
import { Box, Text } from "@chakra-ui/react"

const Logo = (props: any) => {
  return (
    <Box {...props}>
      <Text fontSize="5xl" fontWeight="bold">Bullish</Text>
    </Box>
  )
}

export default Logo;